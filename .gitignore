.venv/
__pycache__/
.claude/

# Generated by training runs, not source -- large, binary, changes every run.
# Exception: final_model.keras is the actual trained state we want to carry
# forward and keep training from, so it's tracked despite the pattern above.
*.keras
!final_model.keras
training_log.txt
GameScore.txt
